## QuecPanelTemplate ChangesLog

### 20250417

-   `EnumTslModel` `BooleanTslModel` `NumberTslModel` 增加工具函数
-   增加 `http-response` 全局类型类型
-   更新 `README.md`
-   统一页面名称

### 20250226

-   添加 `.eslintignore`;
-   修复下发多物模型时 `options` 判断错误;
-   取消 `Text, TextInput` 字体大小随着系统变化;
-   禁用 `RTL`;
-   调整文件名称;

### 20250207

-   增加面板本地打包脚本;
-   调整 `loading` 文件夹名称;

### 20250120

-   `useTslWriter` 增加节流;
-   增加 `useGoBack` hook;
-   调整 `hooks` 文件名称;
-   调整 `config` 文件名称;
-   调整 `store` 文件名称;
-   调整 `types` 文件名称;
-   调整 `utils` 文件名称;
-   调整 `components` 文件名称;

### 20250116

-   `useTslWriter` 增加物模型批量下发;
-   调整 `router screenOptions` 的传递方式;
-   移除 `router header` 相关的配置;
-   增加 `useNavigation` Hooks;
-   `commonHeader` 增加 `Android` 物理返回监听;
